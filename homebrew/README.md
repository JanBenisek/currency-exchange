# Homebrew Formula

This directory contains the Homebrew formula for `cex`. Users can install via:

```bash
brew tap janbenisek/currency-exchange
brew install cex
```

The formula is automatically updated by GitHub Actions on each release.
